import React, { useState } from 'react';

export interface Props {
}

export const Card: React.FC<Props> = ({
}) => {
    return (
        <div>
            Card
        </div>
    )
  }

  export default Card;
