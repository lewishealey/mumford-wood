import React, { useState } from 'react';

// navigation keys
export interface Props {
}

export const Keys: React.FC<Props> = ({
}) => {
    return (
        <div>
            Keys
        </div>
    )
  }

  export default Keys;
