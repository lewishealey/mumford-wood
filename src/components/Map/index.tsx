import React, { useState } from 'react';

export interface Props {
}

export const Map: React.FC<Props> = ({
}) => {
    return (
        <div>
            Map
        </div>
    )
  }

  export default Map;
