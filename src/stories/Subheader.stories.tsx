import SubHeader from '@components/SubHeaderRange';

export default {
  title: 'Components/SubHeader',
  component: SubHeader,
}

const Template = (args) => <SubHeader {...args} />;
export const Playground = Template.bind({});
